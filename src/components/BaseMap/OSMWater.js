var Cesium = require('cesium/Cesium');

// Función para configurar agua - version ligera
export const setupWater = (viewer) => {
  // Sistema desactivado - el WMS es demasiado pesado
  console.log('Water layer system disabled for better performance');
};
